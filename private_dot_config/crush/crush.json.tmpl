{
  "$schema": "https://charm.land/crush.json",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "^bash$",
        "command": "{{ .chezmoi.homeDir }}/.config/crush/hooks/rtk-rewrite.sh"
      }
    ]
  }
}
